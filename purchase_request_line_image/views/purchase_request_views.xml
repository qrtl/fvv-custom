<?xml version="1.0" ?>
<odoo>
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="inherit_id" ref="purchase_request.view_purchase_request_form" />
        <field name="arch" type="xml">
            <xpath expr="//group[@name='specifications']" position="inside">
                <field
                    name="item_image_ids"
                    context="{'default_name': name, 'default_product_id': product_id, 'search_default_product_id': product_id}"
                    mode="tree"
                    nolabel="1"
                />
            </xpath>
            <xpath
                expr="//field[@name='line_ids']/tree/field[@name='name']"
                position="after"
            >
                <field name="item_image_count" string=" " />
                <button
                    name="action_get_item_image_view"
                    string="View Images"
                    type="object"
                    icon="fa-paperclip"
                />
            </xpath>
        </field>
    </record>
</odoo>
